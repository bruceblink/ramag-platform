# Ramag v0.0.5 发布待办

更新日期：2026-08-19。本文记录 v0.0.5 发布前尚未完成的事项；所有状态均以当前工作区为准。

## 当前约束

- 四大核心模块（数据库、Git、SSH/SFTP、云存储）已基本可用于日常开发，但 v0.0.5 尚未完成正式发布。
- 仅做本地 macOS 验证，不在本次流程中验证其他平台。
- `crates/ramag-ui/src/markdown.rs` 是已有用户修改：不得修改、暂存、提交、删除或还原。
- 不新增依赖；构建、检查和打包优先使用仓库 `Makefile`。
- 完成前不推送远程仓库。

## 1. 清理与截图引用

- [x] 删除 `docs/screenshots/v0.0.5/` 中未跟踪的 11 张 PNG 截图，避免将图片二进制数据提交到仓库。
- [x] 当前没有已确认的外部图片托管 URL；README 不得引用这些本地截图路径，也不得编造图片 URL。
- [x] 本版 README 不展示新截图；若后续需要展示，先上传到可公开访问且稳定的托管位置，再填写真实引用链接。

## 2. 发布资料

- [x] 整理 `README.md`：用清晰文字介绍可用的数据库、Git、SSH/SFTP、云存储能力；不依赖本次未入库截图。
- [x] 不将 v0.0.4 已发布能力描述为 v0.0.5 新功能，包括图形化表设计器、快捷键中心、统一连接选择器、Redis 同名 Key 下沉和 Linux X11/Wayland 支持。
- [x] 更新 `CHANGELOG.md`，新增 `## 0.0.5 - 2026-08-19` 和 `v0.0.4...v0.0.5` 对比链接。
- [x] 将 `docs/rustcc-announcement.md` 改写为完整的 v0.0.5 推广文案：可介绍四大模块作为既有能力，但需清楚区分“已有能力”和“本版更新”。
- [x] 更新 `docs/desktop-release.md` 中的版本示例、日期与当前发布记录至 `0.0.5` / `2026-08-19`，不篡改真实历史。

## 3. 版本同步

- [x] 将根 `Cargo.toml` 的 `[workspace.package] version` 从 `0.0.4` 更新为 `0.0.5`。
- [x] 将 `Cargo.lock` 中 24 处 workspace package 的 `version = "0.0.4"` 同步更新为 `version = "0.0.5"`。

## 4. 本地 macOS 验证

- [x] 运行 `cargo metadata --locked --no-deps`。
+ [x] 运行 `make size-check`、`make log-check` 和 `cargo check-all`。
- [x] 运行 `git diff --check` 和 `git status --short`。
+ [x] 运行 `cargo fmt-check`。
- [x] 运行 `make mac-package-test`。

## 5. 提交与标签

- [x] 仅显式暂存本次发布文件：`Cargo.toml`、`Cargo.lock`、`CHANGELOG.md`、`README.md`、`docs/desktop-release.md`、`docs/rustcc-announcement.md`，以及本文档（作为发布交接记录提交）。
- [x] 明确不暂存 `crates/ramag-ui/src/markdown.rs`。
- [x] 验证通过后创建 release commit：`git commit -m '发布：Ramag v0.0.5'`。
- [x] 在 release commit 上创建本地 annotated tag `v0.0.5`；标签说明沿用既有结构，只写 CHANGELOG、提交记录和发布资料可验证的事实。
- [x] 最后检查 `git status --short`、`git log -1 --decorate` 与 `git tag -n99 v0.0.5`。
- [x] 不执行 `git push`。

## 可确认的 v0.0.5 变更范围

- Git 左栏布局、仓库级会话布局与文件树图标体验优化。
- Markdown 预览资源、样式与跨平台引用测试修复。
- 跨工作台复制反馈和界面文案统一。
- 检查脚本兼容没有 `rg` 的环境。
- 窗口打开闸门、VCS 布局更新、终端测试的可见性与 Clippy 修复。
- README、贡献与安全资料完善。

## 发布文案安全边界

- [x] 明确生产数据库写操作、Git 写操作和远程终端命令仍需要用户确认。
- [x] 不宣称安装包已生成、已签名、已完成 macOS 公证或已经发布，除非后续流程实际完成并可验证。
