[package]
name = "ramag-tool-dbclient"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Ramag DB Client 工具：实现 Tool trait + 提供数据库客户端 GUI"

[lints]
workspace = true

[dependencies]
ramag-domain = { workspace = true }
ramag-app = { workspace = true }
ramag-ui = { workspace = true }
# 关系型 DB 共享层：复用 split_statements / inject_limit_if_needed（按 driver 切换 dollar-quoted 等方言选项）
ramag-infra-sql-shared = { workspace = true }
# Redis 视图组件（dbclient 是统一连接入口；Redis 会话面板在此 crate 内复用）
ramag-tool-redis = { workspace = true }
# MongoDB 视图组件（同上，dbclient 统一入口下用 SessionEntity::Mongo 装载）
ramag-tool-mongodb = { workspace = true }

# SQL 补全的 anyhow::Result 返回值
anyhow = { workspace = true }

gpui-kit = { workspace = true }

# 异步
futures = { workspace = true }
smol = { workspace = true }
async-trait = { workspace = true }

# Action 派生需要
serde = { workspace = true }
# 打开中的连接列表偏好序列化（workspace 既有依赖复用）
serde_json = { workspace = true }
schemars = { workspace = true }

# 时间显示（历史面板）
chrono = { workspace = true }
# 迁移审批记录只保存 SQL 的 SHA-256 指纹，不保存脚本正文
sha2 = { workspace = true }
hex = { workspace = true }
# 系统文件对话框（导出 CSV / JSON）
rfd = { workspace = true }

# SQL 自动补全（gpui_kit::component::CompletionProvider trait 接口要求）
lsp-types = { workspace = true }
ropey = { workspace = true }

# schema cache 用 RwLock（parking_lot 比 std 更轻 / 无毒）
parking_lot = { workspace = true }

# SQL 格式化：纯 Rust，~MIT
sqlformat = "0.3"

# 日志
tracing = { workspace = true }

[dev-dependencies]
# 结果表滚动回归测试：仅测试构建启用 headless 窗口与输入模拟。
gpui-kit = { workspace = true, features = ["test-support"] }
async-trait = { workspace = true }
ramag-infra-mongodb = { workspace = true }
ramag-infra-redis = { workspace = true }
ramag-infra-sqlite = { workspace = true }
